import { DifficultyStarsPipe } from './difficulty-stars.pipe';

describe('DifficultyStarsPipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyStarsPipe();
    expect(pipe).toBeTruthy();
  });
});
